//
//  TestView.swift
//  MapMe
//
//  Created by Crypto on 11/28/24.
//

import SwiftUI
import MapboxMaps
/*
// testing to see if custom style loads using SwiftUI
struct TestView: View {
    var body: some View {
        
        // Safely unwrap the StyleURI
        if let customStyle = StyleURI(rawValue: "mapbox://styles/fr3shtricks/cm3w53d4700c601rdgzjp53h9") {
            Map()
                .mapStyle(MapStyle(uri: customStyle)) // Use the unwrapped StyleURI
                .ignoresSafeArea()
        } else {
            Text("Invalid style URI")
        }
         
    }
}

#Preview {
    TestView()
}
*/

